--!nonstrict
-- Placeholder for the prebuilt ReactRoblox (react-lua renderer) single-file bundle.
--
-- Replace this file's contents with a bundled build of jsdotlua/react-lua's
-- `ReactRoblox` package (createRoot, createPortal, act, ...).
--
-- Alternatively, inject at runtime before loading Syphon:
--   getgenv().SyphonReactRoblox = <ReactRoblox table>
error('Syphon: ReactRoblox bundle not installed — see packages/README.md', 0)
