--!nonstrict
-- Tab — a page within a Window, holds groupboxes on left/right columns.
-- Scaffold: no UI yet.
local Env = require('core/env')

-- props: { Name: string, children: ... }
local function Tab(props)
    local React = Env.React
    -- TODO: render tab button + content columns; render props.children.
    return nil
end

return Tab
