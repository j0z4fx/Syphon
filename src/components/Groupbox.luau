--!nonstrict
-- Groupbox — titled container that stacks controls vertically. Scaffold: no UI yet.
local Env = require('core/env')

-- props: { Name: string, Side: number?, children: ... }
local function Groupbox(props)
    local React = Env.React
    -- TODO: render bordered box + title + vertical list of props.children.
    return nil
end

return Groupbox
